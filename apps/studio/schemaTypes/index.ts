import {artistType} from './artistType'
import {eventType} from './eventTypes'
import {navbarType} from './navbarType'
import {venueType} from './venueType'

export const schemaTypes = [eventType, artistType, venueType, navbarType]
